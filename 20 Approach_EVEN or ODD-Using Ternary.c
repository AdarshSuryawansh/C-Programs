#include<stdio.h>
#include<conio.h>
int main()
{
    int Num==0;

    printf("\n Enter an integer to check whether EVEN or ODD=");
    scanf("%d",&Num);

    if(Num==0)
    {
        printf("\n Given number is neutral.");
    }
    else
    {
     (Num%2==0)? printf("\n Given number is EVEN."): printf("\n given number is ODD.");
    }
      printf (\n\t Thanks !!!)
}
